version: "3.8"
services:
  obs-app:
    build: .
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app:rw
    environment:
      - PYTHONUNBUFFERED=1
